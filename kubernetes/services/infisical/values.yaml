controllerManager:
  manager:
    resources:
      requests:
        cpu: 5m
        # Idles ~73Mi; a controller-runtime manager spikes well above that
        # when resyncing many InfisicalSecrets at once.
        memory: 128Mi
      limits:
        cpu: null
        memory: 256Mi
