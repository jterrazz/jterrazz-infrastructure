# Global HTTPS Redirect for Traefik
# Ensures all HTTP traffic is redirected to HTTPS

---
# IngressRoute for global HTTP to HTTPS redirect
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: global-http-redirect
  namespace: default
spec:
  entryPoints:
    - web
  routes:
    - match: "Host(`app.local`) || Host(`portainer.local`) || Host(`argocd.local`)"
      kind: Rule
      middlewares:
        - name: https-redirect
      services:
        - name: default-landing
          port: 80
