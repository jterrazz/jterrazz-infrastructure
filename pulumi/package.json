{
  "name": "jterrazz-infrastructure",
  "version": "1.0.0",
  "description": "k3s cluster infra — an OrbStack VM on the dev Mac, plus its Cloudflare DNS",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "@pulumi/cloudflare": "^6.20.0",
    "@pulumi/command": "^1.2.1",
    "@pulumi/pulumi": "^3.260.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^6.0.3"
  }
}
